import React from 'react';

export default function Dtiboundedsvg() {
  return (
    <div className="dti-boundedsvg">
    </div>
  );
}

if (process.env.NODE_ENV !== 'production') {
  Dtiboundedsvg.propTypes = {
  };
}
